/* 

    var có thể redeclare. còn let và const thì không
    var chỉ bị giới hạn bởi function scope, còn không thì nó sẽ là global
    let và const thì bị giới hạn bởi block scope (if, for...)
    hoisting không áp dụng cho let và const
*/