### Docker Compose comes to make setting up environment easier
### Docker Compose can create one-line start environment

##### Start environment
`docker-compose up`

##### Close environment
`docker-compose down`