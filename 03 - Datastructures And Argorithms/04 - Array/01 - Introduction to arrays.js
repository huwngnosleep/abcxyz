// push, pop
// việc push vào array là thêm vào index cuối cùng, không cần động chạm gì tới các phần tử trước
// nên là 0(1)

// unshift (thêm vào đầu mảng)
// để thêm một phần tử vào đầu array, bản chất là phải thay thế từng cái index bằng phần tử mới
// nên là 0(n)